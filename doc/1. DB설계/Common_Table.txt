DROP TABLE COMMON_USER;

CREATE TABLE COMMON_USER (
	/* 2018.06.25 정기현 추가 - 사용자 입력 항목													*/
	USER_EMAIL		VARCHAR(100)    NOT NULL					COMMENT '이메일아이디'		,
	USER_PW			VARCHAR(100)	NOT NULL					COMMENT '비밀번호'		,
	USER_NAME		VARCHAR(50)		NOT NULL					COMMENT '이름'			,
	ZIP_CODE		VARCHAR(10)	    NULL						COMMENT '우편번호'		,
	ADDR1			VARCHAR(400)    NULL						COMMENT '주소1'			,
	ADDR2			VARCHAR(400)    NULL						COMMENT '주소2'			,
	PHONE			VARCHAR(50)	    NULL						COMMENT '전화번호'		,
	/* 2018.06.25 정기현 추가 - 관리자 권한 항목													*/
	SUPER_MANAGER	VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT 'SUPER관리자여부'	,
	HOME_MANAGER	VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT 'HOME관리자여부'	,
	MALL_MANAGER	VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT 'MALL관리자여부'	,
	ITSM_MANAGER	VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT 'ITSM관리자여부'	,
	/* 2018.06.25 정기현 추가 - 모든 테이블 공통 항목												*/
	REG_ID			VARCHAR(100)	NOT NULL					COMMENT '작성자ID'		,
	REG_IP			VARCHAR(20)		NOT NULL					COMMENT '작성자IP'		,
	REG_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '작성일'			,
	MOD_ID			VARCHAR(100)	NOT NULL					COMMENT '수정자ID'		,
	MOD_IP			VARCHAR(20)		NOT NULL					COMMENT '수정자IP'		,
	MOD_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '수정일'			,
	DEL_YN			VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT '삭제여부'		,
	DEL_ID			VARCHAR(100)	NOT NULL					COMMENT '삭제자ID'		,
	DEL_IP			VARCHAR(20)		NOT NULL					COMMENT '삭제자IP'		,
	DEL_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '삭제일'			,
	PRIMARY KEY (USER_EMAIL)
) COMMENT '사용자공통테이블';

//COMMON_USER_LOGIN_LOG
//COMMON_USER_MENU_LOG

DROP TABLE COMMON_CODE;

CREATE TABLE COMMON_CODE (
	/* 2018.06.25 정기현 추가 - 공통 코드 항목													*/
	CODE			VARCHAR(100)	NOT NULL					COMMENT '코드'			,
	UP_CODE			VARCHAR(100)	NULL						COMMENT '상위코드'		,
	CODE_KOR_NAME	VARCHAR(200)	NOT NULL					COMMENT '한국어코드명'		,
	CODE_ENG_NAME	VARCHAR(200)	NULL						COMMENT '영어코드명'		,
	CODE_JP_NAME	VARCHAR(200)	NULL						COMMENT '일어코드명'		,
	CODE_CN_NAME	VARCHAR(200)	NULL						COMMENT '중국어코드명'		,
	/* 2018.06.25 정기현 추가 - 모든 테이블 공통 항목												*/
	REG_ID			VARCHAR(100)	NOT NULL					COMMENT '작성자ID'		,
	REG_IP			VARCHAR(20)		NOT NULL					COMMENT '작성자IP'		,
	REG_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '작성일'			,
	MOD_ID			VARCHAR(100)	NOT NULL					COMMENT '수정자ID'		,
	MOD_IP			VARCHAR(20)		NOT NULL					COMMENT '수정자IP'		,
	MOD_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '수정일'			,
	DEL_YN			VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT '삭제여부'		,
	DEL_ID			VARCHAR(100)	NOT NULL					COMMENT '삭제자ID'		,
	DEL_IP			VARCHAR(20)		NOT NULL					COMMENT '삭제자IP'		,
	DEL_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '삭제일'			,
	PRIMARY KEY (CODE)
) COMMENT '공통코드테이블';

DROP TABLE COMMON_BOARD_CODE;

CREATE TABLE COMMON_BOARD_CODE (
	/* 2018.06.25 정기현 추가 - 게시판 공통 코드 항목												*/
	SITE_LANG		VARCHAR(20)		NOT NULL	DEFAULT 'KOR'	COMMENT '사이트언어'		,
	BOARD_CODE		VARCHAR(100)	NOT NULL					COMMENT '게시판코드'		,
	BOARD_UP_CODE	VARCHAR(100)	NULL						COMMENT '게시판상위코드'	,
	BOARD_NAME		VARCHAR(200)	NOT NULL					COMMENT '게시판명'		,
	BOARD_ORDER		INT				NOT NULL	DEFAULT 1		COMMENT '게시판순서'		,
	OPEN_YN			VARCHAR(1)		NOT NULL	DEFAULT 'Y'		COMMENT '게시판공개여부'	,
	MANAGER_YN		VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT '관리자전용게시판여부',
	EDITOR_YN		VARCHAR(1)		NOT NULL	DEFAULT 'Y'		COMMENT '에디터사용여부'	,
	FILE_YN			VARCHAR(1)		NOT NULL	DEFAULT 'Y'		COMMENT '첨부파일사용여부'	,
	REPLY_YN		VARCHAR(1)		NOT NULL	DEFAULT 'Y'		COMMENT '리플사용여부'		,
	/* 2018.06.25 정기현 추가 - 모든 테이블 공통 항목												*/
	REG_ID			VARCHAR(100)	NOT NULL					COMMENT '작성자ID'		,
	REG_IP			VARCHAR(20)		NOT NULL					COMMENT '작성자IP'		,
	REG_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '작성일'			,
	MOD_ID			VARCHAR(100)	NOT NULL					COMMENT '수정자ID'		,
	MOD_IP			VARCHAR(20)		NOT NULL					COMMENT '수정자IP'		,
	MOD_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '수정일'			,
	DEL_YN			VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT '삭제여부'		,
	DEL_ID			VARCHAR(100)	NOT NULL					COMMENT '삭제자ID'		,
	DEL_IP			VARCHAR(20)		NOT NULL					COMMENT '삭제자IP'		,
	DEL_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '삭제일'			,
	PRIMARY KEY (BOARD_CODE)
) COMMENT '게시판코드테이블';

DROP TABLE COMMON_BOARD;

CREATE TABLE COMMON_BOARD (
	/* 2018.06.25 정기현 추가 - 게시판 항목													*/
	BOARD_CODE		VARCHAR(20)		NOT NULL					COMMENT '게시판코드'		,
	BOARD_NO		INT				NOT NULL					COMMENT '글번호'			,
	BOARD_UP_NO		INT				NULL						COMMENT '글상위번호'		,
	USER_ID			VARCHAR(20)		NULL						COMMENT '작성자ID'		,
	TITLE			VARCHAR(400)	NULL						COMMENT '글제목'			,
	CONTENT			TEXT			NULL						COMMENT '글내용'			,
	OPEN_YN			VARCHAR(1)		NOT NULL	DEFAULT 'Y'		COMMENT '공개여부'		,
	/* 2018.06.25 정기현 추가 - 모든 테이블 공통 항목												*/
	REG_ID			VARCHAR(100)	NOT NULL					COMMENT '작성자ID'		,
	REG_IP			VARCHAR(20)		NOT NULL					COMMENT '작성자IP'		,
	REG_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '작성일'			,
	MOD_ID			VARCHAR(100)	NOT NULL					COMMENT '수정자ID'		,
	MOD_IP			VARCHAR(20)		NOT NULL					COMMENT '수정자IP'		,
	MOD_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '수정일'			,
	DEL_YN			VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT '삭제여부'		,
	DEL_ID			VARCHAR(100)	NOT NULL					COMMENT '삭제자ID'		,
	DEL_IP			VARCHAR(20)		NOT NULL					COMMENT '삭제자IP'		,
	DEL_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '삭제일'			,
	PRIMARY KEY (BOARD_NO)
) COMMENT '게시판공통테이블';

DROP TABLE COMMON_FILE;

CREATE TABLE COMMON_FILE (
	/* 2018.06.25 정기현 추가 - 첨부파일 항목													*/
	FILE_NO			INT				NOT NULL					COMMENT '파일번호'		,
	FILE_CODE		VARCHAR(20)		NOT NULL					COMMENT '파일코드'		,
	FILE_NAME		VARCHAR(400)	NOT NULL					COMMENT '파일이름'		,
	FILE_PATH		VARCHAR(400)	NOT NULL					COMMENT '파일경로'		,
	FILE_SIZE		INT				NOT NULL	DEFAULT 0		COMMENT '파일크기'		,
	/* 2018.06.25 정기현 추가 - 모든 테이블 공통 항목												*/
	REG_ID			VARCHAR(100)	NOT NULL					COMMENT '작성자ID'		,
	REG_IP			VARCHAR(20)		NOT NULL					COMMENT '작성자IP'		,
	REG_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '작성일'			,
	MOD_ID			VARCHAR(100)	NOT NULL					COMMENT '수정자ID'		,
	MOD_IP			VARCHAR(20)		NOT NULL					COMMENT '수정자IP'		,
	MOD_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '수정일'			,
	DEL_YN			VARCHAR(1)		NOT NULL	DEFAULT 'N'		COMMENT '삭제여부'		,
	DEL_ID			VARCHAR(100)	NOT NULL					COMMENT '삭제자ID'		,
	DEL_IP			VARCHAR(20)		NOT NULL					COMMENT '삭제자IP'		,
	DEL_DATE		DATETIME		NOT NULL	DEFAULT NOW()	COMMENT '삭제일'			,
	PRIMARY KEY (FILE_NO)
) COMMENT '파일공통테이블';